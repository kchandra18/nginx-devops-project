name: CI Pipeline - Build Test Secure

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    name: Build Stage
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Build step
        run: |
          echo "Building application..."
          echo "Build completed ✅"

  test:
    name: Test Stage
    runs-on: ubuntu-latest
    needs: build

    steps:
      - name: Run tests
        run: |
          echo "Running tests..."
          echo "All tests passed ✅"

  security:
    name: Security Scan
    runs-on: ubuntu-latest
    needs: test

    steps:
      - name: Security scan
        run: |
          echo "Running security checks..."
          echo "No vulnerabilities found 🔒"
